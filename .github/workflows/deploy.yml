name: Deploy to GitHub Pages
on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install Playwright browsers
        run: |
          for i in 1 2; do
            echo "Installing Playwright browsers (attempt $i/2)…"
            pnpm exec playwright install --with-deps && exit 0
            echo "Attempt $i failed — likely a flaky apt/Chrome mirror on the runner."
            sleep 5
          done
          echo "Both attempts failed; giving up."
          exit 1
      - run: pnpm run build
      - name: Cross-browser regression check
        run: |
          pnpm run preview &
          for i in $(seq 1 30); do
            curl -sf http://localhost:4321 > /dev/null && break
            sleep 1
          done
          pnpm exec playwright test
      - uses: actions/upload-pages-artifact@v5
        with:
          path: ./dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
