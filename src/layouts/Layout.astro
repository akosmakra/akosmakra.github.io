---
import { basics } from "@cv";
import Vengeance from "@/layouts/Vengeance.astro";
import "@/styles/global.css";

interface Props {
	title: string;
}

const { title } = Astro.props;

const { name, image, email, metaDescription, url, theme, profiles } = basics;

const absoluteImageUrl = new URL(image, url).toString();
const ogImageUrl = new URL("/og-image.png", url).toString();
const ogImageAlt = `${name} — Test Automation Engineer`;

const personLd = {
	"@context": "https://schema.org",
	"@type": "Person",
	name,
	email,
	image: absoluteImageUrl,
	url,
	jobTitle: "Test Automation Engineer",
	sameAs: profiles.map((profile) => profile.url),
};
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title>{title}</title>
		<meta name="description" content={metaDescription} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preload" as="image" href={image} />
		<link rel="canonical" href={url} />

		<meta property="og:url" content={url} />
		<meta property="og:type" content="website" />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={metaDescription} />
		<meta property="og:image" content={ogImageUrl} />
		<meta property="og:image:width" content="1200" />
		<meta property="og:image:height" content="630" />
		<meta property="og:image:alt" content={ogImageAlt} />

		<meta name="twitter:card" content="summary_large_image" />
		<meta property="twitter:domain" content={url.split("//")[1] ?? ""} />
		<meta property="twitter:url" content={url} />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={metaDescription} />
		<meta name="twitter:image" content={ogImageUrl} />
		<meta name="twitter:image:alt" content={ogImageAlt} />

		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Itim" />

		<script is:inline type="application/ld+json" set:html={JSON.stringify(personLd)} />

		{import.meta.env.PROD && <script is:inline data-goatcounter="https://akosmakra.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>}

		<Vengeance />
	</head>
	<body data-theme={theme} class="relative bg-skin-fill">
		<div class="absolute top-0 -z-10 h-full w-full print:hidden">
			<div class="bg-blob bg-skin-hue/10 absolute bottom-auto left-[20%] right-auto top-0 h-[200px] w-[200px] rounded-full blur-[80px] invert-0 dark:bg-white/5"></div>
		</div>
		<slot />
		<style is:global>
			body,
			figure {
				margin: 0;
				padding: 0;
			}

			a {
				text-decoration: none;
			}

			ul {
				list-style: none;
				margin: 0;
				padding: 0;
			}

			*,
			*::before,
			*::after {
				box-sizing: border-box;
			}

			h1,
			h2,
			h3,
			h4 {
				color: var(--color-text-base);
				margin: 0;
				font-family:
					system-ui,
					-apple-system,
					BlinkMacSystemFont,
					"Segoe UI",
					Roboto,
					Oxygen,
					Ubuntu,
					Cantarell,
					"Open Sans",
					"Helvetica Neue",
					sans-serif;
			}

			p {
				font-size: 0.875rem;
				line-height: 1.25rem;
				color: var(--color-text-muted);
				line-height: 1.5;
				text-wrap: pretty;
			}

			.print {
				display: none !important;
			}

			/* Plain `transform`, not Tailwind's `-translate-x-[…]`/`translate-y-[…]` utilities:
			   Tailwind v4 compiles those to `translate:var(--tw-translate-x)var(--tw-translate-y)`
			   with no space between the two var() calls, which Firefox silently drops. */
			.bg-blob {
				transform: translate(-30%, 20%);
			}

			.dev * {
				border: 1px solid rgb(239 68 68 / 0.5);
				background-color: rgb(239 68 68 / 0.05);
			}

			#hotkeypad [data-backdrop] {
				background-color: rgb(39 39 42) !important;
				opacity: 0.8 !important;
			}

			@media print {
				.no-print {
					display: none !important;
				}

				.print {
					display: block !important;
				}

				astro-dev-toolbar {
					display: none !important;
				}

				article {
					break-inside: avoid;
				}
			}
		</style>

		<script>
			import Alpine from "alpinejs";
			import collapse from "@alpinejs/collapse";

			Alpine.plugin(collapse);

			// Theme selector component
			Alpine.data("themeSelector", () => ({
				open: false,
				currentTheme: "system",
				themes: [
					{ value: "system", label: "System", icon: "computer" },
					{ value: "light", label: "Light", icon: "sun" },
					{ value: "dark", label: "Dark", icon: "moon" },
				],

				init() {
					const savedTheme = localStorage.getItem("theme");
					this.currentTheme = savedTheme || "system";
					this.applyTheme(this.currentTheme);

					window
						.matchMedia("(prefers-color-scheme: dark)")
						.addEventListener("change", () => {
							if (this.currentTheme === "system") {
								this.applyTheme("system");
							}
						});
				},

				selectTheme(value: string) {
					this.currentTheme = value;
					this.applyTheme(value);
					localStorage.setItem("theme", value);
					this.open = false;
				},

				applyTheme(value: string) {
					const systemTheme = window.matchMedia("(prefers-color-scheme: dark)")
						.matches
						? "dark"
						: "light";
					const theme = value === "system" ? systemTheme : value;
					document.documentElement.classList.remove("light", "dark");
					document.documentElement.classList.add(theme);
				},
			}));

			Alpine.start();
		</script>
	</body>
</html>
